{
  "name": "ai-agent-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent Monorepo",
  "scripts": {
    "dev": "pnpm --filter @ai-agent/desktop dev",
    "build": "pnpm --filter @ai-agent/desktop build",
    "build:all": "pnpm -r build",
    "dev:server": "pnpm --filter @ai-agent/server dev"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
