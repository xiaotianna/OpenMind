import { ConfigTitle } from '@/components/setting/config-title';

export default function SettingsMemoryPage() {
  return (
    <>
      <section>
        <ConfigTitle>Memory</ConfigTitle>
      </section>
    </>
  )
}
